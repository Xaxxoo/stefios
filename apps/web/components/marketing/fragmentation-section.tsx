'use client';

import { useState } from 'react';
import { cn } from '../../lib/utils';

const systems = [
  ['Wallet', 'Public accounts'], ['Tokenized fund', 'RWA exposure'], ['Lending market', 'Borrow / supply'], ['DEX', 'Asset exchange'],
  ['Liquidity pool', 'Market making'], ['Payment rail', 'Settlement'], ['Bridge', 'Network movement'], ['Anchor', 'On / off-ramp'],
] as const;

export function FragmentationSection() {
  const [active, setActive] = useState<string | null>(null);
  return <section className="relative overflow-hidden border-y border-white/[0.06] bg-[hsl(var(--surface-1))] px-5 py-24 sm:px-8 sm:py-32"><div className="mx-auto max-w-7xl"><div className="max-w-2xl"><p className="text-xs uppercase tracking-[0.16em] text-[hsl(var(--accent))]">The fragmented financial stack</p><h2 className="mt-4 text-4xl font-semibold tracking-tight sm:text-6xl">Your capital shouldn’t need fifteen apps.</h2><p className="mt-5 max-w-xl text-base leading-7 text-[hsl(var(--muted))]">Balances live in one place. Lending in another. A bridge, an anchor, a DEX, and a separate spreadsheet to understand the risk. The system is connected. Your view of it should be too.</p></div><div className="relative mt-16 grid gap-12 lg:min-h-[440px] lg:grid-cols-[1fr_220px_1fr] lg:items-center"><div className="grid grid-cols-2 gap-3 sm:grid-cols-4 lg:grid-cols-2">{systems.map(([name, detail], index) => <button key={name} onClick={() => setActive(active === name ? null : name)} className={cn('fragment-node group rounded-xl border p-4 text-left transition-colors', active === name ? 'border-[hsl(var(--accent))]/50 bg-[hsl(var(--accent))]/10' : 'border-white/[0.08] bg-white/[0.025] hover:border-white/20 hover:bg-white/[0.05]')}><span className="flex h-8 w-8 items-center justify-center rounded-lg border border-white/10 bg-[hsl(var(--surface-3))] text-xs text-[hsl(var(--accent))]">{String(index + 1).padStart(2, '0')}</span><span className="mt-3 block text-sm font-medium">{name}</span><span className="mt-1 block text-[11px] text-[hsl(var(--muted))]">{detail}</span></button>)}</div><div className="relative flex h-36 items-center justify-center lg:h-full"><svg className="pointer-events-none absolute inset-0 h-full w-full overflow-visible" viewBox="0 0 220 440" preserveAspectRatio="none" aria-hidden="true"><path className="fragment-flow" d="M0 74 C85 74 115 165 220 220" /><path className="fragment-flow fragment-flow-delay" d="M0 154 C85 154 115 190 220 220" /><path className="fragment-flow" d="M0 286 C85 286 115 250 220 220" /><path className="fragment-flow fragment-flow-delay" d="M0 366 C85 366 115 275 220 220" /></svg><div className="relative flex h-28 w-28 items-center justify-center rounded-full border border-[hsl(var(--accent))]/50 bg-[hsl(var(--accent))]/10 text-center shadow-[0_0_70px_hsl(var(--accent))/0.16)]"><div><span className="block text-2xl font-semibold text-[hsl(var(--accent))]">S</span><span className="mt-1 block text-[10px] font-medium uppercase tracking-[0.12em]">Financial<br />OS</span></div></div></div><div className="rounded-2xl border border-[hsl(var(--accent))]/20 bg-[hsl(var(--accent))]/[0.045] p-6 shadow-[0_0_70px_hsl(var(--accent))/0.06)]"><p className="text-xs uppercase tracking-[0.14em] text-[hsl(var(--accent))]">One command center</p><h3 className="mt-3 text-2xl font-semibold">Stellar Financial OS</h3><p className="mt-4 text-sm leading-6 text-[hsl(var(--muted))]">A normalized view across your accounts, assets, protocols, payments, and capital movements.</p><div className="mt-6 space-y-3 text-xs text-[hsl(var(--muted))]"><p><span className="mr-2 text-[hsl(var(--positive))]">●</span>Portfolio context across every position</p><p><span className="mr-2 text-[hsl(var(--positive))]">●</span>Wallet remains the signing authority</p><p><span className="mr-2 text-[hsl(var(--positive))]">●</span>One activity trail for every movement</p></div></div></div></div></section>;
}
